{
  "name": "school-notes",
  "version": "1.0.0",
  "description": "Simple MEAN Stack Application to save your school notes.",
  "main": "",
  "scripts": {
    "install:client": "cd client && npm install",
    "install:server": "cd server && npm install",
    "install:everything": "npm run install:client && npm run install:server",
    "start": "npm run install:everything && concurrently \"cd server && npm start\" \"cd client && npm start\""
  },
  "author": "davismiler",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^9.1.0"
  }
}
